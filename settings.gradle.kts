rootProject.name = "sopt-makers-backend"

include(
    "api",
    "domain:domain-user",
    "domain:domain-auth",
    "domain:domain-playground",
    "domain:domain-crew",
    "domain:domain-app",
    "domain:domain-admin",
    "domain:domain-official",
    "storage",
    "clients",
    "core"
)
